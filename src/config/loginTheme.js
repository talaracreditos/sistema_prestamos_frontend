/**
 * Tema visual activo de la pantalla de Login (carrusel + imágenes de
 * bienvenida/recuperación). Cambia este valor para que se carguen todas
 * las imágenes de otra carpeta (assets/img/themes/<tema>/). Si el valor
 * no existe en TEMAS_DISPONIBLES, se cae de vuelta a 'defecto' — ver
 * utilities/data/loginThemeAssets.js.
 *
 * Valores disponibles: 'defecto' | 'navidad'
 */
export const TEMA_LOGIN = 'navidad';